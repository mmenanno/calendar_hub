<div class="min-h-screen bg-slate-950 text-slate-100">
  <header class="bg-slate-900/70 backdrop-blur sticky top-0 z-10 border-b border-slate-800">
    <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
      <div class="flex items-center gap-3">
        <span class="inline-flex h-10 w-10 items-center justify-center rounded-lg bg-indigo-500/20 text-indigo-300">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 6v6l4 2" /></svg>
        </span>
        <div>
          <h1 class="text-xl font-semibold">Settings</h1>
          <p class="text-sm text-slate-400">Defaults and Apple Calendar destination.</p>
        </div>
      </div>
      <%= render "shared/top_nav", active: :settings %>
    </div>
  </header>

  <section class="mx-auto w-full max-w-4xl px-6 py-8">
    <%= render "shared/flashes" %>

    <div class="grid gap-8 lg:grid-cols-[1.2fr,1fr]">
      <%= form_with model: @settings, url: settings_path, data: { controller: "dirty-form", action: "turbo:submit-end->dirty-form#reset" }, html: { autocomplete: "off", "data-1p-ignore": true, "data-lpignore": true } do |f| %>
        <div class="space-y-4" id="settings-form">
          <div class="rounded-2xl border border-slate-800 bg-slate-900/70 p-5">
            <h3 class="mb-3 text-base font-semibold">Defaults</h3>
            <div class="space-y-3">
              <div>
                <%= f.label(:default_time_zone, class: "block text-[11px] font-semibold uppercase tracking-wide text-slate-400") %>
                <div class="select-chevron">
                  <%= f.time_zone_select(:default_time_zone, ActiveSupport::TimeZone.all, {}, class: select_class) %>
                </div>
                <p class="mt-1 text-xs text-slate-400">Used when a source doesn’t specify a time zone.</p>
              </div>
              <div>
                <%= f.label(:default_calendar_identifier, "Apple Calendar Identifier", class: "block text-[11px] font-semibold uppercase tracking-wide text-slate-400") %>
                <%= f.text_field(:default_calendar_identifier, placeholder: "e.g. Family, Work, clinic@example.com", class: input_class, data: { controller: "trim", trimTarget: "field", action: "blur->trim#apply" }, autocomplete: "off") %>
                <p class="mt-1 text-xs text-slate-400">If set, new sources will default to this destination calendar. You can override per source.</p>
              </div>
            </div>
          </div>

          <div class="rounded-2xl border border-slate-800 bg-slate-900/70 p-5">
            <h3 class="mb-2 text-base font-semibold">Base URL</h3>
            <p class="mb-3 text-xs text-slate-400">This URL is embedded into Apple Calendar events so links open back to Calendar Hub (e.g., for Tailscale access). If blank, we use Rails’ default_url_options or environment variables.</p>
            <div class="grid gap-3 md:grid-cols-3">
              <div>
                <%= f.label(:app_protocol, class: "mb-1 block text-[11px] font-semibold uppercase tracking-wide text-slate-400") %>
                <div class="select-chevron">
                  <%= f.select(:app_protocol, [["https", "https"], ["http", "http"]], {}, class: select_class) %>
                </div>
              </div>
              <div>
                <%= f.label(:app_host, class: "mb-1 block text-[11px] font-semibold uppercase tracking-wide text-slate-400") %>
                <%= f.text_field(:app_host, placeholder: "hub.tail123.ts.net or localhost", class: input_class, data: { controller: "trim", trimTarget: "field", action: "blur->trim#apply" }, autocomplete: "off", autocapitalize: "none", spellcheck: "false", "data-1p-ignore": true, "data-lpignore": true) %>
              </div>
              <div>
                <%= f.label(:app_port, "Port", class: "mb-1 block text-[11px] font-semibold uppercase tracking-wide text-slate-400") %>
                <%= f.number_field(:app_port, placeholder: "e.g. 3000", class: input_class) %>
                <p class="mt-1 text-xs text-slate-500">Leave blank for default (443 for https, 80 for http). For localhost, set 3000.</p>
              </div>
            </div>
          </div>

          <div class="rounded-2xl border border-slate-800 bg-slate-900/70 p-5">
            <h3 class="mb-2 text-base font-semibold">Apple Calendar Authentication</h3>
            <p class="mb-3 text-xs text-slate-400">Set a global Apple ID username and app‑specific password used for CalDAV. These credentials are used for all sync operations.</p>
            <div class="grid gap-3 md:grid-cols-2">
              <div>
                <%= f.label(:apple_username, class: "mb-1 block text-[11px] font-semibold uppercase tracking-wide text-slate-400") %>
                <%= f.text_field(:apple_username, class: input_class, data: { controller: "trim", trimTarget: "field", action: "blur->trim#apply" }, autocomplete: "off", inputmode: "email", autocapitalize: "none", spellcheck: "false", "data-1p-ignore": true, "data-lpignore": true) %>
              </div>
              <div>
                <%= f.label(:apple_app_password, "App‑Specific Password", class: "mb-1 block text-[11px] font-semibold uppercase tracking-wide text-slate-400") %>
                <%= f.password_field(:apple_app_password, placeholder: (@settings.apple_app_password.present? ? "••••••••" : ""), autocomplete: "new-password", class: input_class, "data-1p-ignore": true, "data-lpignore": true, autocapitalize: "none", spellcheck: "false") %>
                <p class="mt-1 text-xs text-slate-500">Leave blank to keep the existing password.</p>
              </div>
            </div>
            <p class="mt-3 text-xs text-slate-500">Create a new App‑Specific Password at appleid.apple.com → Sign‑In & Security → App‑Specific Passwords.</p>
            <div class="mt-2">
              <button type="button" data-controller="auth-test" data-action="click->auth-test#run" class="cursor-pointer inline-flex items-center rounded-lg border border-slate-700 px-3 py-2 text-sm text-slate-200 hover:border-slate-500">Test Connection</button>
            </div>
          </div>
        </div>

        <button type="submit" data-dirty-form-target="save" class="hidden fixed bottom-4 right-4 z-20 rounded-full bg-indigo-500 px-5 py-3 text-sm font-medium text-white shadow-lg shadow-indigo-500/20 hover:bg-indigo-400">
          Save Changes
        </button>
      <% end %>

      <div class="space-y-4">
        <div class="rounded-2xl border border-slate-800 bg-slate-900/70 p-5" data-controller="collapsible" data-collapsible-key-value="settings-advanced">
          <div class="mb-2 flex items-center justify-between">
            <h3 class="text-base font-semibold">Advanced</h3>
            <button type="button" class="cursor-pointer rounded-lg border border-slate-700 px-2 py-1 text-xs text-slate-200 hover:border-slate-500" data-action="collapsible#toggle">
              <span data-collapsible-target="label">Show</span>
              <span data-collapsible-target="icon" class="ml-1 inline-block transition-transform">▾</span>
            </button>
          </div>
          <div data-collapsible-target="content" class="hidden">
            <div class="space-y-3 text-sm">
              <div>
                <p class="text-slate-300">Verify realtime updates and Cable connectivity.</p>
                <%= link_to "Open Realtime Diagnostics", realtime_path, class: "inline-block rounded-lg border border-slate-700 px-3 py-2 text-slate-200 hover:border-slate-500" %>
              </div>
              <div class="rounded-lg border border-slate-800 bg-slate-900/60 p-3 text-xs text-slate-300">
                <% opts = UrlOptions.for_links %>
                <p class="mb-1"><span class="text-slate-400">Effective Base URL:</span> <code><%= opts[:protocol] || "http" %>://<%= opts[:host] %><%= opts[:port] ? ":#{opts[:port]}" : "" %></code></p>
                <p class="mb-1"><span class="text-slate-400">Rails env:</span> <%= Rails.env %>; <span class="text-slate-400">Ruby:</span> <%= RUBY_VERSION %>; <span class="text-slate-400">Rails:</span> <%= Rails.version %></p>
              </div>
              <div>
                <%= button_to "Reset to Defaults", reset_settings_path, method: :post, form: { data: { turbo_stream: true, controller: "confirm", confirm_message_value: "Reset all settings to defaults?" } }, class: "cursor-pointer inline-flex items-center rounded-lg border border-rose-800 px-3 py-2 text-sm text-rose-200 hover:border-rose-600" %>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>
</div>
